<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.inventario_rfid">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Inventario_RFID.NoActionBar">
        <activity android:name=".Recuperar_contrasena_activity"></activity>
        <activity android:name=".Registrarse_activity" />
        <activity android:name=".ConfiguracionActivity" />
        <activity android:name=".Reportes_activity" />
        <activity
            android:name=".Scaner_activity"
            android:exported="true"
            android:label="Scanner" />
        <activity
            android:name=".Sincronizacion_activity"
            android:exported="true"
            android:label="Sincronizacion" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Inventario_RFID.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>